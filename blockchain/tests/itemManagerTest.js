const { expect } = require('chai')
const { ethers } = require('hardhat')

describe('Test progressive instances of onlyItemManager modifier', async () => {
  let txData, owner, signer

  before(async () => {

  })

  it('allows the itemManager to add an item', async () => {

  })

  it('allows Item Manager to add an item type of SVG', async () => {
    
  })
})